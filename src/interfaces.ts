export interface IBook {
  name: string,
  country: string,
  authors: string[],
  characters: string[],
  isbn: string,
  mediaType: string,
  numberOfPages: number,
  povCharacters: string[],
  publisher: string,
  released: string,
  url: string,  
}
